# Points at the deployed Render backend by default — this app doesn't run a
# local backend; local dev, and Vercel, both talk to the same Render API.
VITE_API_URL=https://nexus-8dwn.onrender.com/api
VITE_SOCKET_URL=https://nexus-8dwn.onrender.com
VITE_MAP_API_KEY=
